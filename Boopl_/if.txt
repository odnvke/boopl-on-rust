// ################  УСЛОВИЯ  ################

// переменная v = true;
v T;
v2 F;


//если v == true;
IF V;
    v  F;
    v2 T;
E; // конец условия


// переменная temp = v2;
temp v2;
IF temp;
    v T; 
E;

//v F;

// условие
IF v;
    P T;

    // вложенное условие
    IF v2; 
        P T;
        G P.end;
    E;

P F;
G P.end; // перходим к концу

E; // конец условие

// 'иначе'
P F; P F;





P.end;
E;